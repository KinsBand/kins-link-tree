---
---

<div class="modal-backdrop hidden" id="feedbackModal" role="dialog" aria-modal="true" aria-labelledby="feedbackModalTitle">
  <div class="modal-content feedback-modal-enhanced">
    <div class="modal-header">
      <div class="modal-title-group">
        <div class="feedback-header-icon-circle">
          <i class="fa-solid fa-lightbulb" id="feedbackHeaderIcon"></i>
        </div>
        <div class="modal-title-text-wrap">
          <h3 id="feedbackModalTitle">Suggest Improvement</h3>
          <p class="modal-subtitle-text">Help us make the Kins site better</p>
        </div>
      </div>
      <button class="close-modal-btn" id="closeFeedbackModal" aria-label="Close feedback modal">
        <i class="fa-solid fa-xmark"></i>
      </button>
    </div>

    <div class="modal-body feedback-modal-body">
      <!-- Form View -->
      <form id="siteFeedbackForm" class="site-feedback-form">
        
        <!-- Feedback Type Selector -->
        <div class="form-group">
          <label class="form-label">Feedback Type</label>
          <div class="type-pills-row" id="feedbackTypeRow">
            <button type="button" class="type-pill-btn active" data-type="Improvement / Idea">
              <i class="fa-solid fa-lightbulb"></i>
              <span>Improvement</span>
            </button>
            <button type="button" class="type-pill-btn" data-type="Bug / Broken Item">
              <i class="fa-solid fa-bug"></i>
              <span>Bug / Issue</span>
            </button>
            <button type="button" class="type-pill-btn" data-type="Content Fix / Typo">
              <i class="fa-solid fa-pen-nib"></i>
              <span>Content Fix</span>
            </button>
          </div>
          <input type="hidden" name="feedbackType" id="selectedFeedbackTypeInput" value="Improvement / Idea">
        </div>

        <!-- Section / Category Selection (2-Row Horizontal Scroll Grid on Mobile) -->
        <div class="form-group">
          <div class="form-label-row">
            <label class="form-label">Which section?</label>
            <span class="category-selected-label" id="categoryDisplayLabel">General Site</span>
          </div>
          <div class="category-scroll-wrapper">
            <div class="category-chips-grid" id="feedbackCategoryGrid">
              <button type="button" class="category-chip active" data-category="General Site">
                <i class="fa-solid fa-globe"></i> General Site
              </button>
              <button type="button" class="category-chip" data-category="Gig Map">
                <i class="fa-solid fa-map-location-dot"></i> Gig Map
              </button>
              <button type="button" class="category-chip" data-category="Search Covers">
                <i class="fa-solid fa-magnifying-glass"></i> Search Covers
              </button>
              <button type="button" class="category-chip" data-category="Share Menu">
                <i class="fa-solid fa-share-nodes"></i> Share Menu
              </button>
              <button type="button" class="category-chip" data-category="Socials & Streams">
                <i class="fa-solid fa-music"></i> Socials & Streams
              </button>
              <button type="button" class="category-chip" data-category="Band Members">
                <i class="fa-solid fa-users"></i> Band Members
              </button>
              <button type="button" class="category-chip" data-category="Subscribe & Contacts">
                <i class="fa-solid fa-envelope"></i> Subscribe & Contacts
              </button>
              <button type="button" class="category-chip" data-category="What Inspires Us">
                <i class="fa-solid fa-fire"></i> What Inspires Us
              </button>
              <button type="button" class="category-chip" data-category="Kins Crew Spotlight">
                <i class="fa-solid fa-clapperboard"></i> Kins Crew
              </button>
              <button type="button" class="category-chip" data-category="Audio Player">
                <i class="fa-solid fa-play"></i> Audio Player
              </button>
            </div>
            <div class="scroll-fade-edge-right"></div>
          </div>
          <input type="hidden" name="feedbackCategory" id="selectedCategoryInput" value="General Site">
        </div>

        <!-- Details Textarea with Adaptable Label & Tabular 0/1000 Counter -->
        <div class="form-group">
          <div class="form-label-row form-label-row-details">
            <label class="form-label dynamic-details-label" for="feedbackDetailsInput" id="detailsLabel">What improvement would you like to see?</label>
            <span class="char-counter" id="charCounter">0 / 1000</span>
          </div>
          <div class="textarea-wrapper">
            <textarea 
              id="feedbackDetailsInput" 
              name="details" 
              rows="3" 
              maxlength="1000" 
              placeholder="Tell us what you'd like added, changed, or refined..." 
              required
            ></textarea>
          </div>
        </div>

        <!-- Screenshot / Image Proof Field with Native Label-Triggered Skeleton Space -->
        <div class="form-group attachment-form-group">
          <div class="attachment-header-row">
            <label class="form-label" for="feedbackScreenshotInput">
              Screenshot / Proof <span class="optional-tag">(Optional)</span>
            </label>
            <span class="attachment-size-badge" id="attachmentSizeHint">Max 5MB</span>
          </div>

          <!-- Hidden Native File Input -->
          <input 
            type="file" 
            id="feedbackScreenshotInput" 
            name="screenshot" 
            accept="image/png, image/jpeg, image/webp, image/gif" 
            class="file-input-hidden" 
          />

          <!-- Native Label Dropzone (Clicking anywhere reliably opens file selector on all platforms) -->
          <label 
            for="feedbackScreenshotInput" 
            class="image-upload-skeleton" 
            id="imageUploadSkeleton" 
            tabindex="0" 
            role="button" 
            aria-label="Attach screenshot or image proof"
          >
            <div class="upload-skeleton-left">
              <div class="upload-icon-circle">
                <i class="fa-solid fa-camera"></i>
              </div>
              <div class="upload-text-wrap">
                <span class="upload-title">Attach Screenshot or Visual Proof</span>
                <span class="upload-subtext">Click to browse or drop image (PNG, JPG, WebP)</span>
              </div>
            </div>
            <div class="browse-btn-pill">
              <i class="fa-solid fa-folder-open"></i> Browse
            </div>
          </label>

          <!-- Image Preview Box (Takes the exact skeleton space upon upload, keeping thumbnail small) -->
          <div class="image-preview-container hidden" id="imagePreviewContainer">
            <div class="preview-left-box">
              <div class="preview-thumb-wrap">
                <img id="feedbackPreviewImg" src="" alt="Screenshot preview" />
              </div>
              <div class="preview-meta-wrap">
                <span class="preview-filename-text" id="previewFileName">screenshot.png</span>
                <div class="preview-badge-row">
                  <span class="attached-success-badge"><i class="fa-solid fa-circle-check"></i> Attached</span>
                  <span class="preview-filesize-text" id="previewFileSize">1.2 MB</span>
                </div>
              </div>
            </div>
            <button type="button" class="remove-image-btn" id="removeImageBtn" title="Remove screenshot" aria-label="Remove screenshot">
              <i class="fa-solid fa-trash-can"></i>
              <span>Remove</span>
            </button>
          </div>
        </div>

        <!-- Optional Contact / Handle -->
        <div class="form-group">
          <label class="form-label" for="feedbackContactInput">
            Your Discord, social, or email <span class="optional-tag">(Optional)</span>
          </label>
          <div class="input-with-icon">
            <i class="fa-solid fa-at input-left-icon"></i>
            <input 
              type="text" 
              id="feedbackContactInput" 
              name="contact" 
              placeholder="e.g. @username or email for follow-up"
              autocomplete="off"
            >
          </div>
        </div>

        <!-- Submit Button -->
        <div class="form-actions">
          <button type="submit" class="submit-feedback-btn" id="submitFeedbackBtn">
            <span class="btn-text">Send Improvement</span>
            <i class="fa-solid fa-paper-plane btn-icon"></i>
            <div class="btn-spinner hidden" id="feedbackSpinner"></div>
          </button>
        </div>
      </form>

      <!-- Success View -->
      <div class="feedback-success-view hidden" id="feedbackSuccessView">
        <div class="success-icon-circle">
          <i class="fa-solid fa-circle-check"></i>
        </div>
        <h4>Thanks for the feedback!</h4>
        <p>Your note has been sent directly to our Discord team.</p>
        
        <div class="discord-cta-wrap">
          <a 
            href="https://discord.gg/Yu2npHUrH" 
            target="_blank" 
            rel="noopener noreferrer" 
            class="discord-cta-btn" 
            id="feedbackDiscordCtaBtn"
          >
            <i class="fa-brands fa-discord"></i>
            <span>Join our Discord</span>
            <i class="fa-solid fa-arrow-up-right-from-square discord-launch-icon"></i>
          </a>
        </div>

        <div class="success-actions">
          <button type="button" class="reset-feedback-btn" id="submitAnotherFeedbackBtn">
            <i class="fa-solid fa-rotate-left"></i> Suggest Another
          </button>
          <button type="button" class="close-done-btn" id="closeFeedbackDoneBtn">
            Done
          </button>
        </div>
      </div>
    </div>
  </div>
</div>

<style>
  .modal-backdrop {
    position: fixed;
    inset: 0;
    width: 100vw;
    height: 100vh;
    background: rgba(0, 0, 0, 0.78);
    backdrop-filter: blur(8px);
    -webkit-backdrop-filter: blur(8px);
    z-index: 9999;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 12px;
    box-sizing: border-box;
    opacity: 1;
    visibility: visible;
    transition: opacity 0.22s cubic-bezier(0.16, 1, 0.3, 1), visibility 0.22s;
  }

  .modal-backdrop.hidden {
    opacity: 0;
    visibility: hidden;
    pointer-events: none;
  }

  .feedback-modal-enhanced {
    width: 100%;
    max-width: 490px;
    max-height: 92vh;
    overflow-y: auto;
    background: rgba(18, 18, 22, 0.97);
    border: 1px solid rgba(255, 255, 255, 0.14);
    border-radius: 16px;
    box-shadow: 0 24px 60px rgba(0, 0, 0, 0.75), 0 0 0 1px rgba(255, 255, 255, 0.05);
    display: flex;
    flex-direction: column;
    box-sizing: border-box;
    scrollbar-width: thin;
    scrollbar-color: rgba(255, 255, 255, 0.2) transparent;
    animation: modalPopIn 0.25s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .feedback-modal-enhanced::-webkit-scrollbar {
    width: 5px;
  }

  .feedback-modal-enhanced::-webkit-scrollbar-track {
    background: transparent;
  }

  .feedback-modal-enhanced::-webkit-scrollbar-thumb {
    background: rgba(255, 255, 255, 0.2);
    border-radius: 4px;
  }

  .feedback-modal-enhanced::-webkit-scrollbar-thumb:hover {
    background: rgba(255, 255, 255, 0.35);
  }

  @keyframes modalPopIn {
    from {
      transform: scale(0.96) translateY(6px);
      opacity: 0;
    }
    to {
      transform: scale(1) translateY(0);
      opacity: 1;
    }
  }

  /* Adaptable Header & Title */
  .modal-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 14px 16px;
    gap: 10px;
    border-bottom: 1px solid rgba(255, 255, 255, 0.08);
    position: sticky;
    top: 0;
    background: rgba(18, 18, 22, 0.98);
    backdrop-filter: blur(14px);
    z-index: 10;
  }

  .modal-title-group {
    display: flex;
    align-items: center;
    gap: 10px;
    min-width: 0;
    flex: 1;
  }

  .feedback-header-icon-circle {
    width: 34px;
    height: 34px;
    border-radius: 9px;
    background: rgba(245, 158, 11, 0.15);
    border: 1px solid rgba(245, 158, 11, 0.35);
    color: #f59e0b;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1rem;
    flex-shrink: 0;
    transition: all 0.2s ease;
  }

  .modal-title-text-wrap {
    min-width: 0;
    flex: 1;
    display: flex;
    flex-direction: column;
    justify-content: center;
  }

  .modal-title-group h3 {
    margin: 0;
    font-family: var(--font-heading, system-ui);
    font-size: clamp(0.92rem, 3.8vw, 1.12rem);
    font-weight: 800;
    color: #ffffff;
    letter-spacing: -0.01em;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
    line-height: 1.2;
  }

  .modal-subtitle-text {
    margin: 2px 0 0 0;
    font-family: var(--font-secondary, system-ui);
    font-size: clamp(0.68rem, 2.6vw, 0.74rem);
    color: var(--text-muted, #94a3b8);
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
    line-height: 1.2;
  }

  .close-modal-btn {
    background: rgba(255, 255, 255, 0.06);
    border: 1px solid rgba(255, 255, 255, 0.1);
    color: #cbd5e1;
    width: 30px;
    height: 30px;
    border-radius: 8px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 0.85rem;
    cursor: pointer;
    flex-shrink: 0;
    transition: all 0.18s ease;
  }

  .close-modal-btn:hover {
    background: rgba(255, 255, 255, 0.16);
    color: #ffffff;
  }

  .feedback-modal-body {
    padding: 16px;
    display: flex;
    flex-direction: column;
    gap: 14px;
  }

  .site-feedback-form {
    display: flex;
    flex-direction: column;
    gap: 14px;
  }

  .form-group {
    display: flex;
    flex-direction: column;
    gap: 6px;
  }

  .form-label-row {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 8px;
  }

  .form-label-row-details {
    align-items: baseline;
  }

  .form-label {
    font-family: var(--font-secondary, system-ui);
    font-size: 0.75rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: #cbd5e1;
  }

  .dynamic-details-label {
    flex: 1;
    min-width: 0;
    line-height: 1.35;
    font-size: clamp(0.72rem, 2.7vw, 0.77rem);
    text-transform: none;
    letter-spacing: 0;
    font-weight: 600;
    color: #e2e8f0;
  }

  .category-selected-label {
    font-family: var(--font-secondary, system-ui);
    font-size: 0.72rem;
    font-weight: 700;
    color: #f59e0b;
    white-space: nowrap;
  }

  .optional-tag {
    font-weight: normal;
    text-transform: none;
    color: #64748b;
    font-size: 0.7rem;
  }

  /* Compact & Balanced Feedback Type Cards */
  .type-pills-row {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 6px;
  }

  .type-pill-btn {
    background: rgba(255, 255, 255, 0.04);
    border: 1px solid rgba(255, 255, 255, 0.09);
    color: #94a3b8;
    padding: 8px 6px;
    min-height: 48px;
    border-radius: 9px;
    font-family: var(--font-secondary, system-ui);
    font-size: clamp(0.68rem, 2.5vw, 0.74rem);
    font-weight: 600;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 4px;
    cursor: pointer;
    transition: all 0.18s ease;
    text-align: center;
    box-sizing: border-box;
  }

  .type-pill-btn span {
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
    max-width: 100%;
    line-height: 1.1;
  }

  .type-pill-btn i {
    font-size: 0.9rem;
    line-height: 1;
  }

  .type-pill-btn:hover {
    background: rgba(255, 255, 255, 0.08);
    color: #ffffff;
    border-color: rgba(255, 255, 255, 0.2);
  }

  .type-pill-btn.active {
    background: rgba(245, 158, 11, 0.16);
    border-color: rgba(245, 158, 11, 0.5);
    color: #fbbf24;
    font-weight: 700;
  }

  .type-pill-btn[data-type="Bug / Broken Item"].active {
    background: rgba(239, 68, 68, 0.16);
    border-color: rgba(239, 68, 68, 0.5);
    color: #f87171;
  }

  .type-pill-btn[data-type="Content Fix / Typo"].active {
    background: rgba(59, 130, 246, 0.16);
    border-color: rgba(59, 130, 246, 0.5);
    color: #60a5fa;
  }

  /* 2-Row Horizontal Scroll Grid for Sections */
  .category-scroll-wrapper {
    position: relative;
    width: 100%;
    overflow: hidden;
  }

  .category-chips-grid {
    display: grid;
    grid-template-rows: repeat(2, 32px);
    grid-auto-flow: column;
    grid-auto-columns: max-content;
    gap: 7px 7px;
    overflow-x: auto;
    padding: 2px 20px 4px 2px;
    scrollbar-width: thin;
    scrollbar-color: rgba(255, 255, 255, 0.2) transparent;
    -webkit-overflow-scrolling: touch;
    scroll-snap-type: x proximity;
  }

  .category-chips-grid::-webkit-scrollbar {
    height: 4px;
  }

  .category-chips-grid::-webkit-scrollbar-thumb {
    background: rgba(255, 255, 255, 0.2);
    border-radius: 4px;
  }

  .scroll-fade-edge-right {
    position: absolute;
    top: 0;
    right: 0;
    bottom: 4px;
    width: 28px;
    background: linear-gradient(90deg, transparent, rgba(18, 18, 22, 0.95));
    pointer-events: none;
  }

  .category-chip {
    background: rgba(255, 255, 255, 0.04);
    border: 1px solid rgba(255, 255, 255, 0.09);
    color: #94a3b8;
    padding: 0 12px;
    height: 32px;
    border-radius: 16px;
    font-family: var(--font-secondary, system-ui);
    font-size: 0.74rem;
    font-weight: 600;
    display: inline-flex;
    align-items: center;
    gap: 6px;
    cursor: pointer;
    transition: all 0.15s ease;
    white-space: nowrap;
    flex-shrink: 0;
    box-sizing: border-box;
    scroll-snap-align: start;
  }

  .category-chip i {
    font-size: 0.74rem;
    opacity: 0.8;
  }

  .category-chip:hover {
    background: rgba(255, 255, 255, 0.09);
    color: #ffffff;
    border-color: rgba(255, 255, 255, 0.18);
  }

  .category-chip.active {
    background: #ffffff;
    color: #0b0b0e;
    border-color: #ffffff;
    font-weight: 700;
  }

  .category-chip.active i {
    opacity: 1;
    color: #0b0b0e;
  }

  /* Textarea & Inputs */
  .textarea-wrapper textarea {
    width: 100%;
    background: rgba(0, 0, 0, 0.38);
    border: 1px solid rgba(255, 255, 255, 0.12);
    border-radius: 10px;
    padding: 10px 12px;
    color: #ffffff;
    font-family: var(--font-secondary, system-ui);
    font-size: 0.82rem;
    line-height: 1.45;
    resize: vertical;
    box-sizing: border-box;
    transition: border-color 0.18s ease, box-shadow 0.18s ease;
  }

  .textarea-wrapper textarea:focus {
    outline: none;
    border-color: rgba(255, 255, 255, 0.4);
    box-shadow: 0 0 0 3px rgba(255, 255, 255, 0.08);
  }

  .textarea-wrapper textarea::placeholder {
    color: #64748b;
  }

  .char-counter {
    font-family: var(--font-secondary, system-ui);
    font-size: clamp(0.66rem, 2.3vw, 0.72rem);
    color: #64748b;
    white-space: nowrap;
    flex-shrink: 0;
    font-variant-numeric: tabular-nums;
  }

  /* ===================================================
     SCREENSHOT / IMAGE ATTACHMENT SECTION
     (Aspect Ratio Resilient & Fully Responsive)
     =================================================== */
  .attachment-form-group {
    display: flex;
    flex-direction: column;
    gap: 6px;
    position: relative;
    width: 100%;
  }

  .attachment-header-row {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 8px;
  }

  .attachment-size-badge {
    font-family: var(--font-secondary, system-ui);
    font-size: 0.68rem;
    font-weight: 700;
    color: #94a3b8;
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(255, 255, 255, 0.1);
    padding: 2px 7px;
    border-radius: 4px;
    letter-spacing: 0.02em;
    white-space: nowrap;
  }

  .file-input-hidden {
    position: absolute;
    width: 1px;
    height: 1px;
    padding: 0;
    margin: -1px;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    white-space: nowrap;
    border: 0;
    opacity: 0;
    pointer-events: none;
  }

  /* Skeleton Dropzone Card (Semantic Label Element) */
  .image-upload-skeleton {
    width: 100%;
    min-height: 62px;
    background: linear-gradient(135deg, rgba(255, 255, 255, 0.03) 0%, rgba(255, 255, 255, 0.01) 100%);
    border: 1.5px dashed rgba(255, 255, 255, 0.18);
    border-radius: 10px;
    padding: 10px 12px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 10px;
    cursor: pointer;
    box-sizing: border-box;
    transition: all 0.2s cubic-bezier(0.16, 1, 0.3, 1);
    user-select: none;
    outline: none;
    position: relative;
    overflow: hidden;
  }

  .image-upload-skeleton::before {
    content: '';
    position: absolute;
    inset: 0;
    background: radial-gradient(circle at 20% 50%, rgba(245, 158, 11, 0.08), transparent 60%);
    opacity: 0;
    transition: opacity 0.25s ease;
    pointer-events: none;
  }

  .image-upload-skeleton:hover::before,
  .image-upload-skeleton:focus-visible::before {
    opacity: 1;
  }

  .image-upload-skeleton:hover,
  .image-upload-skeleton:focus-visible {
    border-color: rgba(245, 158, 11, 0.45);
    background: rgba(255, 255, 255, 0.05);
    transform: translateY(-1px);
    box-shadow: 0 4px 16px rgba(0, 0, 0, 0.3), 0 0 12px rgba(245, 158, 11, 0.1);
  }

  .image-upload-skeleton.drag-over {
    border-color: #f59e0b;
    border-style: solid;
    background: rgba(245, 158, 11, 0.12);
    box-shadow: 0 0 20px rgba(245, 158, 11, 0.25);
    transform: scale(1.01);
  }

  .upload-skeleton-left {
    display: flex;
    align-items: center;
    gap: 10px;
    min-width: 0;
    flex: 1;
    pointer-events: none;
  }

  .upload-icon-circle {
    width: 36px;
    height: 36px;
    border-radius: 8px;
    background: rgba(245, 158, 11, 0.12);
    border: 1px solid rgba(245, 158, 11, 0.28);
    color: #fbbf24;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 0.95rem;
    flex-shrink: 0;
    transition: all 0.2s ease;
  }

  .image-upload-skeleton:hover .upload-icon-circle {
    background: rgba(245, 158, 11, 0.22);
    border-color: rgba(245, 158, 11, 0.5);
    transform: scale(1.05);
  }

  .upload-text-wrap {
    display: flex;
    flex-direction: column;
    gap: 2px;
    min-width: 0;
    flex: 1;
    text-align: left;
  }

  .upload-title {
    font-family: var(--font-secondary, system-ui);
    font-size: 0.78rem;
    font-weight: 700;
    color: #ffffff;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
    letter-spacing: -0.01em;
  }

  .upload-subtext {
    font-family: var(--font-secondary, system-ui);
    font-size: 0.68rem;
    color: #94a3b8;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }

  .browse-btn-pill {
    background: rgba(255, 255, 255, 0.08);
    border: 1px solid rgba(255, 255, 255, 0.15);
    color: #ffffff;
    font-family: var(--font-secondary, system-ui);
    font-size: 0.72rem;
    font-weight: 700;
    padding: 5px 10px;
    border-radius: 6px;
    white-space: nowrap;
    display: inline-flex;
    align-items: center;
    gap: 5px;
    pointer-events: none;
    flex-shrink: 0;
    transition: all 0.18s ease;
  }

  .image-upload-skeleton:hover .browse-btn-pill {
    background: #ffffff;
    color: #0b0b0e;
    border-color: #ffffff;
  }

  /* Loaded Preview Card (takes exact skeleton space with compact thumbnail) */
  .image-preview-container {
    width: 100%;
    min-height: 62px;
    background: rgba(14, 14, 18, 0.96);
    border: 1px solid rgba(34, 197, 94, 0.4);
    border-radius: 10px;
    padding: 8px 12px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 10px;
    box-sizing: border-box;
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.5), 0 0 12px rgba(34, 197, 94, 0.1);
    animation: previewPopIn 0.22s cubic-bezier(0.16, 1, 0.3, 1);
  }

  @keyframes previewPopIn {
    from {
      opacity: 0;
      transform: scale(0.97);
    }
    to {
      opacity: 1;
      transform: scale(1);
    }
  }

  .preview-left-box {
    display: flex;
    align-items: center;
    gap: 10px;
    min-width: 0;
    flex: 1;
  }

  /* Fixed compact thumbnail container ensuring image stays small regardless of source aspect ratio */
  .preview-thumb-wrap {
    width: 44px;
    height: 44px;
    min-width: 44px;
    max-width: 44px;
    aspect-ratio: 1 / 1;
    border-radius: 8px;
    overflow: hidden;
    border: 1px solid rgba(255, 255, 255, 0.2);
    background: #000000;
    flex-shrink: 0;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .preview-thumb-wrap img {
    width: 100%;
    height: 100%;
    max-width: 44px;
    max-height: 44px;
    object-fit: cover;
    object-position: center;
    display: block;
  }

  .preview-meta-wrap {
    display: flex;
    flex-direction: column;
    gap: 2px;
    min-width: 0;
    flex: 1;
  }

  .preview-filename-text {
    font-family: var(--font-secondary, system-ui);
    font-size: 0.77rem;
    font-weight: 700;
    color: #ffffff;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }

  .preview-badge-row {
    display: flex;
    align-items: center;
    gap: 6px;
    flex-wrap: wrap;
  }

  .attached-success-badge {
    font-family: var(--font-secondary, system-ui);
    font-size: 0.64rem;
    font-weight: 700;
    color: #22c55e;
    background: rgba(34, 197, 94, 0.15);
    border: 1px solid rgba(34, 197, 94, 0.3);
    padding: 1px 5px;
    border-radius: 4px;
    display: inline-flex;
    align-items: center;
    gap: 3px;
    white-space: nowrap;
  }

  .preview-filesize-text {
    font-family: var(--font-secondary, system-ui);
    font-size: 0.68rem;
    color: #94a3b8;
    white-space: nowrap;
  }

  .remove-image-btn {
    background: rgba(239, 68, 68, 0.12);
    border: 1px solid rgba(239, 68, 68, 0.3);
    color: #f87171;
    font-family: var(--font-secondary, system-ui);
    font-size: 0.72rem;
    font-weight: 700;
    padding: 7px 10px;
    border-radius: 7px;
    cursor: pointer;
    display: inline-flex;
    align-items: center;
    gap: 5px;
    flex-shrink: 0;
    transition: all 0.18s ease;
    white-space: nowrap;
  }

  .remove-image-btn:hover {
    background: rgba(239, 68, 68, 0.25);
    border-color: rgba(239, 68, 68, 0.5);
    color: #ffffff;
    transform: translateY(-1px);
    box-shadow: 0 4px 12px rgba(239, 68, 68, 0.25);
  }

  @media (max-width: 380px) {
    .browse-btn-pill {
      display: none;
    }
    .image-upload-skeleton {
      padding: 8px 10px;
    }
    .image-preview-container {
      padding: 6px 10px;
    }
    .remove-image-btn span {
      display: none;
    }
    .remove-image-btn {
      padding: 6px 8px;
    }
  }

  .input-with-icon {
    position: relative;
    display: flex;
    align-items: center;
  }

  .input-left-icon {
    position: absolute;
    left: 12px;
    color: #64748b;
    font-size: 0.82rem;
    pointer-events: none;
  }

  .input-with-icon input {
    width: 100%;
    background: rgba(0, 0, 0, 0.38);
    border: 1px solid rgba(255, 255, 255, 0.12);
    border-radius: 10px;
    padding: 10px 12px 10px 34px;
    color: #ffffff;
    font-family: var(--font-secondary, system-ui);
    font-size: 0.82rem;
    box-sizing: border-box;
    transition: border-color 0.18s ease, box-shadow 0.18s ease;
  }

  .input-with-icon input:focus {
    outline: none;
    border-color: rgba(255, 255, 255, 0.4);
    box-shadow: 0 0 0 3px rgba(255, 255, 255, 0.08);
  }

  .input-with-icon input::placeholder {
    color: #64748b;
  }

  /* Submit Button */
  .form-actions {
    margin-top: 2px;
  }

  .submit-feedback-btn {
    width: 100%;
    background: #ffffff;
    color: #0b0b0e;
    border: none;
    border-radius: 10px;
    padding: 12px 18px;
    font-family: var(--font-secondary, system-ui);
    font-size: 0.82rem;
    font-weight: 800;
    text-transform: uppercase;
    letter-spacing: 0.06em;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
    cursor: pointer;
    transition: transform 0.15s ease, background-color 0.15s ease, box-shadow 0.15s ease;
    box-shadow: 0 4px 14px rgba(255, 255, 255, 0.12);
  }

  .submit-feedback-btn:hover {
    background: #f1f5f9;
    transform: translateY(-1px);
    box-shadow: 0 6px 18px rgba(255, 255, 255, 0.22);
  }

  .submit-feedback-btn:active {
    transform: translateY(0);
  }

  .submit-feedback-btn:disabled {
    opacity: 0.6;
    cursor: not-allowed;
    transform: none !important;
  }

  /* Spinner */
  .btn-spinner {
    width: 15px;
    height: 15px;
    border: 2px solid rgba(0, 0, 0, 0.2);
    border-top-color: #000000;
    border-radius: 50%;
    animation: spin 0.6s linear infinite;
  }

  @keyframes spin {
    to { transform: rotate(360deg); }
  }

  .hidden {
    display: none !important;
  }

  /* Success View */
  .feedback-success-view {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    padding: 16px 8px 8px 8px;
    gap: 12px;
    animation: fadeIn 0.25s ease;
  }

  @keyframes fadeIn {
    from { opacity: 0; transform: translateY(6px); }
    to { opacity: 1; transform: translateY(0); }
  }

  .success-icon-circle {
    width: 48px;
    height: 48px;
    border-radius: 50%;
    background: rgba(34, 197, 94, 0.15);
    border: 1px solid rgba(34, 197, 94, 0.35);
    color: #22c55e;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.5rem;
  }

  .feedback-success-view h4 {
    font-family: var(--font-heading, system-ui);
    font-size: 1.15rem;
    font-weight: 800;
    color: #ffffff;
    margin: 0;
  }

  .feedback-success-view p {
    font-family: var(--font-secondary, system-ui);
    font-size: 0.82rem;
    color: #94a3b8;
    line-height: 1.45;
    margin: 0;
    max-width: 360px;
  }

  .discord-cta-wrap {
    width: 100%;
    margin-top: 4px;
  }

  .discord-cta-btn {
    width: 100%;
    background: #5865f2;
    color: #ffffff;
    border: 1px solid rgba(255, 255, 255, 0.15);
    border-radius: 10px;
    padding: 12px 18px;
    font-family: var(--font-secondary, system-ui);
    font-size: 0.84rem;
    font-weight: 700;
    letter-spacing: 0.03em;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 9px;
    text-decoration: none;
    cursor: pointer;
    box-sizing: border-box;
    transition: transform 0.18s ease, background-color 0.18s ease, box-shadow 0.18s ease;
    box-shadow: 0 4px 18px rgba(88, 101, 242, 0.35);
  }

  .discord-cta-btn:hover {
    background: #4752c4;
    transform: translateY(-1px);
    box-shadow: 0 6px 22px rgba(88, 101, 242, 0.5);
    color: #ffffff;
  }

  .discord-cta-btn:active {
    transform: translateY(0);
  }

  .discord-cta-btn i.fa-discord {
    font-size: 1.1rem;
  }

  .discord-launch-icon {
    font-size: 0.72rem;
    opacity: 0.8;
  }

  .success-actions {
    display: flex;
    gap: 10px;
    margin-top: 2px;
    width: 100%;
  }

  .reset-feedback-btn {
    flex: 1;
    background: rgba(255, 255, 255, 0.08);
    border: 1px solid rgba(255, 255, 255, 0.15);
    color: #ffffff;
    font-family: var(--font-secondary, system-ui);
    font-size: 0.78rem;
    font-weight: 700;
    padding: 10px 12px;
    border-radius: 8px;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 6px;
    transition: background-color 0.18s ease;
  }

  .reset-feedback-btn:hover {
    background: rgba(255, 255, 255, 0.16);
  }

  .close-done-btn {
    flex: 1;
    background: #ffffff;
    color: #0b0b0e;
    border: none;
    font-family: var(--font-secondary, system-ui);
    font-size: 0.78rem;
    font-weight: 800;
    padding: 10px 12px;
    border-radius: 8px;
    cursor: pointer;
    transition: background-color 0.18s ease;
  }

  .close-done-btn:hover {
    background: #e2e8f0;
  }
</style>

<script>
  import { showToast } from '../../scripts/controllers/toast.js';
  import { getEssentialDiagnostics } from '../../scripts/controllers/telemetry.js';

  const modal = document.getElementById('feedbackModal');
  const closeBtn = document.getElementById('closeFeedbackModal');
  const form = document.getElementById('siteFeedbackForm') as HTMLFormElement;
  const successView = document.getElementById('feedbackSuccessView');
  const submitBtn = document.getElementById('submitFeedbackBtn') as HTMLButtonElement;
  const btnText = submitBtn?.querySelector('.btn-text');
  const btnIcon = submitBtn?.querySelector('.btn-icon');
  const spinner = document.getElementById('feedbackSpinner');
  const detailsInput = document.getElementById('feedbackDetailsInput') as HTMLTextAreaElement;
  const charCounter = document.getElementById('charCounter');
  const detailsLabel = document.getElementById('detailsLabel');
  const headerIcon = document.getElementById('feedbackHeaderIcon');
  const categoryDisplayLabel = document.getElementById('categoryDisplayLabel');
  const selectedTypeInput = document.getElementById('selectedFeedbackTypeInput') as HTMLInputElement;
  const selectedCategoryInput = document.getElementById('selectedCategoryInput') as HTMLInputElement;
  const submitAnotherBtn = document.getElementById('submitAnotherFeedbackBtn');
  const doneBtn = document.getElementById('closeFeedbackDoneBtn');

  // Screenshot Upload Elements
  const screenshotInput = document.getElementById('feedbackScreenshotInput') as HTMLInputElement;
  const uploadSkeleton = document.getElementById('imageUploadSkeleton');
  const previewContainer = document.getElementById('imagePreviewContainer');
  const previewImg = document.getElementById('feedbackPreviewImg') as HTMLImageElement;
  const previewFileName = document.getElementById('previewFileName');
  const previewFileSize = document.getElementById('previewFileSize');
  const removeImageBtn = document.getElementById('removeImageBtn');

  let currentAttachedFile: File | null = null;

  // Format File Size
  function formatBytes(bytes: number): string {
    if (bytes === 0) return '0 B';
    const k = 1024;
    const sizes = ['B', 'KB', 'MB', 'GB'];
    const i = Math.floor(Math.log(bytes) / Math.log(k));
    return parseFloat((bytes / Math.pow(k, i)).toFixed(1)) + ' ' + sizes[i];
  }

  // Handle Image Selection
  function handleImageFile(file: File | null) {
    if (!file) return;

    if (!file.type.startsWith('image/')) {
      showToast("Please select a valid image (PNG, JPG, WebP, GIF).");
      return;
    }

    if (file.size > 5 * 1024 * 1024) {
      showToast("Image size exceeds 5MB limit.");
      return;
    }

    currentAttachedFile = file;

    const reader = new FileReader();
    reader.onload = (e) => {
      if (previewImg && e.target?.result) {
        previewImg.src = e.target.result as string;
      }
      if (previewFileName) previewFileName.textContent = file.name;
      if (previewFileSize) previewFileSize.textContent = formatBytes(file.size);

      // Hide skeleton and show preview in the exact same space
      uploadSkeleton?.classList.add('hidden');
      previewContainer?.classList.remove('hidden');
    };
    reader.readAsDataURL(file);
  }

  // Keyboard accessibility for label dropzone
  uploadSkeleton?.addEventListener('keydown', (e) => {
    if (e.key === 'Enter' || e.key === ' ') {
      e.preventDefault();
      screenshotInput?.click();
    }
  });

  // File Input Change listener
  screenshotInput?.addEventListener('change', () => {
    if (screenshotInput.files && screenshotInput.files[0]) {
      handleImageFile(screenshotInput.files[0]);
    }
  });

  // Drag & Drop onto Skeleton
  uploadSkeleton?.addEventListener('dragover', (e) => {
    e.preventDefault();
    uploadSkeleton.classList.add('drag-over');
  });

  uploadSkeleton?.addEventListener('dragleave', () => {
    uploadSkeleton.classList.remove('drag-over');
  });

  uploadSkeleton?.addEventListener('drop', (e) => {
    e.preventDefault();
    uploadSkeleton.classList.remove('drag-over');
    if (e.dataTransfer?.files && e.dataTransfer.files[0]) {
      handleImageFile(e.dataTransfer.files[0]);
    }
  });

  // Remove Attached Image
  function removeAttachedImage() {
    currentAttachedFile = null;
    if (screenshotInput) screenshotInput.value = '';
    if (previewImg) previewImg.src = '';
    previewContainer?.classList.add('hidden');
    uploadSkeleton?.classList.remove('hidden');
  }

  removeImageBtn?.addEventListener('click', (e) => {
    e.preventDefault();
    e.stopPropagation();
    removeAttachedImage();
  });

  // Close handlers
  function closeModal() {
    modal?.classList.add('hidden');
    document.body.classList.remove('modal-open');
  }

  closeBtn?.addEventListener('click', closeModal);
  doneBtn?.addEventListener('click', closeModal);

  modal?.addEventListener('click', (e) => {
    if (e.target === modal) {
      closeModal();
    }
  });

  window.addEventListener('keydown', (e) => {
    if (e.key === 'Escape' && modal && !modal.classList.contains('hidden')) {
      closeModal();
    }
  });

  // Type Pills Selection
  const typePills = document.querySelectorAll('.type-pill-btn');
  typePills.forEach((pill) => {
    pill.addEventListener('click', () => {
      typePills.forEach((p) => p.classList.remove('active'));
      pill.classList.add('active');
      const type = pill.getAttribute('data-type') || 'Improvement / Idea';
      if (selectedTypeInput) selectedTypeInput.value = type;

      // Update placeholders & icons based on type
      if (type.includes('Bug')) {
        if (detailsLabel) detailsLabel.textContent = "What is broken or not working as expected?";
        if (detailsInput) detailsInput.placeholder = "Describe the issue, where it happened, and what went wrong...";
        if (btnText) btnText.textContent = "Report Issue";
        if (headerIcon) {
          headerIcon.className = "fa-solid fa-bug";
          (headerIcon.parentElement as HTMLElement).style.color = "#f87171";
          (headerIcon.parentElement as HTMLElement).style.borderColor = "rgba(239, 68, 68, 0.4)";
          (headerIcon.parentElement as HTMLElement).style.backgroundColor = "rgba(239, 68, 68, 0.15)";
        }
      } else if (type.includes('Content')) {
        if (detailsLabel) detailsLabel.textContent = "What text or information should be corrected?";
        if (detailsInput) detailsInput.placeholder = "Describe the typo, missing credit, or outdated detail...";
        if (btnText) btnText.textContent = "Send Correction";
        if (headerIcon) {
          headerIcon.className = "fa-solid fa-pen-nib";
          (headerIcon.parentElement as HTMLElement).style.color = "#60a5fa";
          (headerIcon.parentElement as HTMLElement).style.borderColor = "rgba(59, 130, 246, 0.4)";
          (headerIcon.parentElement as HTMLElement).style.backgroundColor = "rgba(59, 130, 246, 0.15)";
        }
      } else {
        if (detailsLabel) detailsLabel.textContent = "What improvement would you like to see?";
        if (detailsInput) detailsInput.placeholder = "Tell us what you'd like added, changed, or refined...";
        if (btnText) btnText.textContent = "Send Improvement";
        if (headerIcon) {
          headerIcon.className = "fa-solid fa-lightbulb";
          (headerIcon.parentElement as HTMLElement).style.color = "#f59e0b";
          (headerIcon.parentElement as HTMLElement).style.borderColor = "rgba(245, 158, 11, 0.35)";
          (headerIcon.parentElement as HTMLElement).style.backgroundColor = "rgba(245, 158, 11, 0.15)";
        }
      }
    });
  });

  // Category Selection
  const categoryChips = document.querySelectorAll('.category-chip');
  categoryChips.forEach((chip) => {
    chip.addEventListener('click', () => {
      categoryChips.forEach((c) => c.classList.remove('active'));
      chip.classList.add('active');
      const cat = chip.getAttribute('data-category') || 'General Site';
      if (selectedCategoryInput) selectedCategoryInput.value = cat;
      if (categoryDisplayLabel) categoryDisplayLabel.textContent = cat;
    });
  });

  // Character Counter
  detailsInput?.addEventListener('input', () => {
    const len = detailsInput.value.length;
    if (charCounter) charCounter.textContent = `${len} / 1000`;
  });

  // Form Submission with Essential Diagnostics & Image Attachment
  form?.addEventListener('submit', async (e) => {
    e.preventDefault();
    const textarea = document.getElementById('feedbackDetailsInput') as HTMLTextAreaElement;
    const details = textarea?.value?.trim() || '';

    if (!details) {
      showToast("Please enter feedback details.");
      textarea?.focus();
      return;
    }

    const feedbackType = (document.getElementById('selectedFeedbackTypeInput') as HTMLInputElement)?.value || 'Improvement / Idea';
    const category = (document.getElementById('selectedCategoryInput') as HTMLInputElement)?.value || 'General Site';
    const contact = (document.getElementById('feedbackContactInput') as HTMLInputElement)?.value?.trim() || '';

    // Collect Essential Diagnostics
    const diagnostics = getEssentialDiagnostics();

    // Route to the correct Discord channel based on feedback type
    const webhookMap: Record<string, string> = {
      'Improvement / Idea': 'https://discordapp.com/api/webhooks/1537676700199157862/vgZ-ZSOncntm_jmnRg3EIFl_s2noGib1ALxlCG1Mw_qXnB9wVI_BrsFAN7s3VU02pxpw',
      'Bug / Broken Item': 'https://discordapp.com/api/webhooks/1537794715712491591/o-sh7Hks55I-Cc_X8j6qXfGNMZ8lDP8yCzGg2085_cdEuQiqyKW9NxeeQ9wfNwsYiyax',
      'Content Fix / Typo': 'https://discordapp.com/api/webhooks/1537794718677733417/uaoohPCjEUbQMfwoeOxvHu-pkG8FBRndkAuP6w2VKdhperzqGEdsbddsQLR6E0T-l8z7'
    };
    const webhookUrl = webhookMap[feedbackType] || webhookMap['Improvement / Idea'];

    let embedColor = 0xf59e0b; // Gold for ideas
    let typeEmoji = '💡';
    if (feedbackType.includes('Bug')) {
      embedColor = 0xef4444; // Red for bugs
      typeEmoji = '🐛';
    } else if (feedbackType.includes('Content')) {
      embedColor = 0x3b82f6; // Blue for content
      typeEmoji = '📝';
    }

    const descriptionLines = [
      `**User Message:**`,
      `"${details}"`,
      ``,
      `• **Category:** ${category}`,
      `• **Submitter:** ${contact ? `\`${contact}\`` : '*Anonymous Fan*'}`,
      `• **Viewport:** \`${diagnostics.viewportWithDpr}\``,
      `• **Environment:** ${diagnostics.environment}`,
      `• **Page:** [${diagnostics.url}](${diagnostics.url})`,
      `• **Date:** ${diagnostics.formattedDate}`
    ];

    if (diagnostics.lastError) {
      descriptionLines.push(`• **Last Error:** \`${diagnostics.lastError}\``);
    }

    const discordPayload = {
      username: 'Kins Website Feedback',
      avatar_url: 'https://raw.githubusercontent.com/KinsBand/kins-link-tree/main/pfp.jpg',
      embeds: [
        {
          title: `${typeEmoji} ${feedbackType}: ${category}`,
          description: descriptionLines.join('\n'),
          color: embedColor,
          image: currentAttachedFile ? { url: 'attachment://screenshot.png' } : undefined,
          footer: { text: `Kins Official Website • Build: ${diagnostics.buildVersion}` },
          timestamp: diagnostics.timestamp
        }
      ]
    };

    // UI Loading State
    if (submitBtn) submitBtn.disabled = true;
    btnIcon?.classList.add('hidden');
    spinner?.classList.remove('hidden');

    try {
      let sentSuccess = false;

      // 1. Direct Webhook Dispatch (Multipart with image attachment or JSON)
      try {
        if (currentAttachedFile) {
          const formData = new FormData();
          formData.append('payload_json', JSON.stringify(discordPayload));
          formData.append('files[0]', currentAttachedFile, 'screenshot.png');

          const response = await fetch(webhookUrl, {
            method: 'POST',
            body: formData
          });
          if (response.ok || response.status === 204 || response.status === 200) {
            sentSuccess = true;
          }
        } else {
          const response = await fetch(webhookUrl, {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify(discordPayload)
          });
          if (response.ok || response.status === 204 || response.status === 200) {
            sentSuccess = true;
          }
        }
      } catch (directErr) {
        console.warn("Direct webhook attempt error:", directErr);
      }

      // 2. Server API fallback if direct webhook didn't complete (e.g. client adblocker)
      if (!sentSuccess) {
        try {
          const apiRes = await fetch('/api/feedback', {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({
              feedback: {
                type: feedbackType,
                category: category,
                user_message: details,
                contact: contact
              },
              ...diagnostics
            })
          });
          if (apiRes.ok) sentSuccess = true;
        } catch (apiErr) {
          console.warn("Server API fallback error:", apiErr);
        }
      }

      form.classList.add('hidden');
      successView?.classList.remove('hidden');
      showToast("Thanks! Your feedback has been sent to Discord.");
    } catch (err) {
      console.warn("Feedback network warning:", err);
      form.classList.add('hidden');
      successView?.classList.remove('hidden');
      showToast("Thanks! Your feedback has been received.");
    } finally {
      if (submitBtn) submitBtn.disabled = false;
      btnIcon?.classList.remove('hidden');
      spinner?.classList.add('hidden');
    }
  });

  // Submit Another Button
  submitAnotherBtn?.addEventListener('click', () => {
    form.reset();
    removeAttachedImage();
    if (charCounter) charCounter.textContent = "0 / 1000";
    successView?.classList.add('hidden');
    form.classList.remove('hidden');

    // Reset default selections
    typePills.forEach((p, idx) => {
      if (idx === 0) p.classList.add('active');
      else p.classList.remove('active');
    });
    if (selectedTypeInput) selectedTypeInput.value = 'Improvement / Idea';

    categoryChips.forEach((c, idx) => {
      if (idx === 0) c.classList.add('active');
      else c.classList.remove('active');
    });
    if (selectedCategoryInput) selectedCategoryInput.value = 'General Site';
    if (categoryDisplayLabel) categoryDisplayLabel.textContent = 'General Site';
    if (headerIcon) {
      headerIcon.className = 'fa-solid fa-lightbulb';
      (headerIcon.parentElement as HTMLElement).style.color = "#f59e0b";
      (headerIcon.parentElement as HTMLElement).style.borderColor = "rgba(245, 158, 11, 0.35)";
      (headerIcon.parentElement as HTMLElement).style.backgroundColor = "rgba(245, 158, 11, 0.15)";
    }
    if (btnText) btnText.textContent = 'Send Improvement';
  });
</script>
